# Processed Data Notes

This folder stores intermediate or transformed datasets derived from `data/raw`.

Examples:

- tokenized training text snapshots
- filtered/rebalanced train/eval subsets
- audit-cleaned copies before training runs

Current pipeline reads directly from `data/raw`, so this folder may remain minimal until additional preprocessing stages are added.
